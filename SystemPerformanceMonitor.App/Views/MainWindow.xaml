<Window x:Class="SystemPerformanceMonitor.App.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SystemPerformanceMonitor.App"
        mc:Ignorable="d"
        Title="System Performance Monitor" Height="450" Width="800">
    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="System Performance Monitor"
                   FontSize="28"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30" />

        <TextBlock Grid.Row="1"
                   Text="{Binding CpuUsage, StringFormat=CPU Usage: {0:F1}%}"
                   FontSize="20"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15" />

        <TextBlock Grid.Row="2"
                   Text="{Binding MemoryUsage, StringFormat=Memory Usage: {0:F1}%}"
                   FontSize="20"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30" />

        <TextBlock Grid.Row="3"
                   Text="{Binding Status}"
                   FontSize="20"
                   HorizontalAlignment="Center" />

        <StackPanel Grid.Row="4"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center">
            <Button Content="Start Monitoring"
                    Command="{Binding StartMonitoringCommand}"
                    Margin="5" />

            <Button Content="Stop Monitoring"
                    Command="{Binding StopMonitoringCommand}"
                    Margin="5" />

        </StackPanel>
        
    </Grid>
    
</Window>
